build/
compile_commands.json
*.o
*.a
*.csv
perf.data*
callgrind.out.*
.cache/

# The playground's scratch box: everything except .gitkeep. The files are
# built by `make` just like the rest of playground/, but they are never
# committed — that is the whole difference.
playground/scratch/*
!playground/scratch/.gitkeep
